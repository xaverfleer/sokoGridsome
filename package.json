{
  "name": "soko-gridsome",
  "private": true,
  "scripts": {
    "build": "npm run build:client && npm run build:lambda",
    "build:client": "gridsome build",
    "build:lambda": "netlify-lambda build src/netlify-functions",
    "develop": "gridsome develop",
    "explore": "gridsome explore",
    "test": "tape 'src/**/*Test.js'"
  },
  "dependencies": {
    "@sendgrid/mail": "^7.4.2",
    "crypto": "^1.0.1",
    "encoding": "^0.1.13",
    "faunadb": "^4.0.0",
    "gridsome": "^0.7.0",
    "logrocket": "^1.0.14",
    "netlify-lambda": "^2.0.3"
  },
  "devDependencies": {
    "tape": "^5.1.1"
  }
}
